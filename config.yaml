# ---- GLOBAL PIPELINE SETTINGS ----
# Subject-specific settings (keywords, target_states, etc.) live in subjects/<name>/subject.yaml

# ---- API SETTINGS ----
model: "claude-sonnet-4-20250514"
max_retries: 3
retry_delay_seconds: 5

# ---- TELEGRAM SETTINGS ----
telegram_enabled: true
